spring:
  config:
    activate:
      on-profile: dev

  data:
    mongodb:
      uri: mongodb://localhost:27017/ttms
      database: ttms

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 7d65271df2efc2
    password: 0ed24d729c21c5
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8081
  servlet:
    context-path: /ttms

springdoc:
  swagger-ui:
    path: /ui

rsa:
  public-key: classpath:certs/public.pem
  private-key: classpath:certs/private.pem

com:
  custom:
    reset-link: https://api.cavinkare.in/meetingagenda/api/swagger-ui/index.html
    isSignUp: true
    swagger-path: /ttms/api/
    swagger-live-path: /ttms/api/